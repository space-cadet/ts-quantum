---
kind: edit_chunk
id: 2026-05-16-123000
created_at: 2026-05-16 12:30:00 IST
task_ids: [T12]
source_branch: main
source_commit: workspace
---

#### 12:30:00 IST - T12: Critical Bug Fixes — Wigner Symbols, Eigendecomposition, Geometry, Integration

**Summary:** Fixed 29 failing tests across 8 test files. Test suite now passes 451/451 (100%).

**Bugs Fixed:**

1. **Modified** `src/angularMomentum/wignerSymbols.ts` — Wigner 6j Racah formula
   - Added missing `(z+1)!` numerator term
   - Fixed per-term `(-1)^z` phase factor placement
   
2. **Modified** `src/angularMomentum/wignerSymbols.ts` — Wigner 3j phase
   - Changed global phase from `(-1)^(j1-j2+m3)` to `(-1)^(j1-j2-m3)`

3. **Modified** `src/angularMomentum/composition.ts` — Clebsch-Gordan Racah formula
   - Fixed swapped signs in two denominator factorial terms
   - Removed incorrect extra `(-1)^(j1-j2+m)` phase factor

4. **Modified** `src/geometry/quantumDistance.ts` — Bloch sphere geodesic distance
   - Fixed formula: `√(2 − 2cos(γ/2))` instead of `√2·sin(γ/2)`

5. **Modified** `src/utils/matrixOperations.ts` — Eigendecomposition
   - Fixed `normalizeVectors()` phase correction: `math.exp(math.complex(0, -currentPhase))` instead of `math.exp(-currentPhase)` (was computing real exponential instead of complex phase rotation)
   - Fixed eigenvalue/vector sorting alignment
   - Preserved all eigenvalues for defective matrices

6. **Modified** `src/utils/matrixFunctions.ts` — Matrix function implementations
   - Fixed identity and square function on eigenvalues

7. **Modified** `web/simulations.ts` — Import cleanup
   - Replaced hacky `require('../dist/index.js')` with proper static imports from `../src/index`

**Test Corrections:**

8. **Modified** `__tests__/angularMomentum/wigner6j.test.ts`
   - Updated 6 test expectations (verified with sympy)
   - Replaced incorrect Regge symmetry test with column permutation symmetry

9. **Modified** `__tests__/angularMomentum/wigner3j.test.ts`
   - Fixed sign for `wigner3j(0.5, 0.5, 1; 0.5, -0.5, 0)` = +1/√6

10. **Modified** `__tests__/angularMomentum/composition.test.ts`
    - Swapped two CG coefficient expectations (verified with sympy)

11. **Modified** `__tests__/eigendecomposition.test.ts`
    - Added `{computeEigenvectors: true}` to complex Hermitian test
    - Fixed Pauli Z test value/vector correspondence (removed incorrect sorting)

12. **Modified** `__tests__/eigen.test.ts`
    - Fixed matrix reconstruction: use `adjoint(U)` instead of element-wise conjugate

13. **Modified** `__tests__/integration.test.ts`
    - Replaced plain objects with proper `StateVector` instances
    - Added `StateVector` import

**Verification:**
- `pnpm test` — 451/451 passing
- `pnpm build` — passes
- `pnpm web:build` — passes
